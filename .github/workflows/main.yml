on:
  schedule:
    - cron: '30 0 1 * *'
      timezone: "America/New_York"

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Run task cleanup
        run: |
          curl -sS -f "https://taskpulse/api/cron/cleanup-tasks?days=120&secret=${{ secrets.CRON_SECRET }}"
